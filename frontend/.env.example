# 2026-09-19 起：/admin/* 改用登入密碼的 session 驗證（06-site-auth.md），
# 不再需要把固定 key 嵌進前端 bundle（NEXT_PUBLIC_ROUTER_API_KEY 已拿掉）。
# 這兩個變數刻意不加 NEXT_PUBLIC_ 前綴——只能在伺服器端（middleware、
# Route Handler）讀到，絕對不能曝露給瀏覽器。

# 登入密碼本身，使用者在 /login 輸入這組密碼換 session。
SITE_PASSWORD=

# session token 的簽章金鑰，要跟根目錄後端 .env 的 SITE_SESSION_SECRET
# 填「完全一樣的值」——Next.js（登入時簽發、Middleware 驗證）跟 Express
# 後端（/admin/* 各自獨立驗證）各自實作一份驗證邏輯，共用同一把金鑰才能
# 兩邊都驗證得過同一顆 cookie。產生方式：
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SITE_SESSION_SECRET=
